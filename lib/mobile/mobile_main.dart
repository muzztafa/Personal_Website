import 'package:flutter/material.dart';

class Mobile_Main extends StatelessWidget {
  const Mobile_Main({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("this is a mobile scaf")),
    );
  }
}
